function calcularAreaCuadrado(base, altura) {
    return (base * altura);
  }

  //coloca la base y la altura
  const base = "140";
  const altura = "140";
  const area = calcularAreaCuadrado(base, altura);
  console.log(area);//19600